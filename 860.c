#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int cavalos;
    
    scanf("%d", &cavalos);
    printf("Informe a quantidade de cavalos existentes no haras:\n");
    printf("Numero de ferraduras a serem compradas: %d", (cavalos*4));
	return 0;
}